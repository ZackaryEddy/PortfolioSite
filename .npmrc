# NPM configuration for clean builds on Sevalla
# Prevents tracker conflicts during parallel builds
strict-peer-dependencies=false
engine-strict=false
legacy-peer-deps=false
fund=false
audit=false
progress=false
loglevel=error
# Force foreground mode to prevent tracker conflicts
foreground-scripts=true
# Prefer offline to avoid registry issues
prefer-offline=false
