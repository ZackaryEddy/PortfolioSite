# NPM configuration for clean builds
# Prevents tracker conflicts during parallel builds
strict-peer-dependencies=false
engine-strict=false
legacy-peer-deps=false
fund=false
audit=false
progress=false
loglevel=error
